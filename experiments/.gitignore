# Experiment runs contain raw telemetry, per-example predictions, and checkpoints
# that are too large for source control. Curated paper artifacts are force-added.
*
!.gitignore
# Traverse directories to expose README files without tracking raw artifacts.
!*/
!README.md
